object Firebase {
    private const val sharedVersion = "1.6.2"
    const val firestore = "dev.gitlive:firebase-firestore:$sharedVersion"
//    const val storage = "dev.gitlive:firebase-storage:$version"

    const val androidStorage = "com.google.firebase:firebase-storage:20.1.0"
}